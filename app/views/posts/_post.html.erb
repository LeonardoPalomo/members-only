<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

  <p>
    <% if user_signed_in? %>
      <strong>Made by:</strong>
      <%= post.user.email %>
    <% end %>
  </p>

</div>
