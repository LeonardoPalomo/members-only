Rails project for authentication and autorization testing (using gem Devise)

to run:
$rails db:migrate
$rails s

You must sign up first, go to route /users/sign_up (should automatically log in)
Now you can create posts as the active user.